How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    By going through the lines find by the FC or diff commands and comparing them to find any syntax or typo error

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    It will help in keeping track of the progress of the project or program and help in collaboration

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?

    Automatically saving feature helps to reduce the task of manually doing it but at the same time if during log off something
	gets typed by mistake and is saved which we don't recognise at that instant and when we return later still we aren't able to make out the difference that can be tedious

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    It proves helpful when working with multiple files such as i collaboration

How can you use the commands git log and git diff to view the history of files?

    by typing these commands in the command line followed by the names of the files to compare

How might using version control make you more confident to make changes that
could break something?

    It might give us the confidence to make any changes and not worry about it as we will have the log of the changes made which we can easily undo with the help of the log and obtain our previous stable version in case of crash

Now that you have your workspace set up, what do you want to try using Git for?

    I want to try working on a project with my brother where we both can easily collaborate and track our work